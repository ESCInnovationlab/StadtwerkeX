server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Use Azure's DNS resolver to find the backend URL
    resolver 168.63.129.16 valid=30s;
    set $backend_url https://stadwerkapp-fhe9f2grc3eqgygv.southindia-01.azurewebsites.net;

    # Proxy all /api/ requests to the Azure Backend App Service.
    location /api/ {
        proxy_pass         $backend_url;
        proxy_http_version 1.1;
        proxy_set_header   Host              stadwerkapp-fhe9f2grc3eqgygv.southindia-01.azurewebsites.net;
        proxy_set_header   X-Real-IP         $remote_addr;

        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_read_timeout 120s;
    }

    # SPA fallback — all unknown routes serve index.html (React Router handles them)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets aggressively
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location = /favicon.ico { log_not_found off; access_log off; }
}
